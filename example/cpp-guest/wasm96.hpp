#ifndef WASM96_EXAMPLE_CPP_GUEST_WASM96_HPP
#define WASM96_EXAMPLE_CPP_GUEST_WASM96_HPP

// This example intentionally uses the canonical C++ SDK header from the repo,
// rather than keeping a stale local copy in the example directory.
#include "../../wasm96-cpp-sdk/wasm96.hpp"

#endif // WASM96_EXAMPLE_CPP_GUEST_WASM96_HPP